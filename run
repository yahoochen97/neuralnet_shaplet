#!/bin/bash
declare -a datasets=("Adiac" "BeetleFly" "Coffee" "Earthquakes" "Gun_Point" "OSULeaf" "Plane" "Strawberry" "Worms")

mkdir -p results

for dataset in ${datasets[@]}; do
    bsub -R '(!gpu) span[hosts=1]' -G SEAS-Lab-Garnett -J "${dataset}" "./onerun $dataset"
done